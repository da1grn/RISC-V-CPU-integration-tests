# Интеграционные тесты для RISC-V процессора

В этой директории находится набор инструментов для автоматического тестирования вашего процессора на Verilog. В отличие от unit-тестов, которые проверяют отдельные модули, эти тесты запускают полноценные ассемблерные программы, проверяя работу всего процессора целиком (Control Unit, ALU, память, переходы).

**Весь код тестов написан с помощью gemini 3 pro и незначительно уточнен автором.** 
За сим, принимаются улучшения, дополнения, исправления. С этими целями вы можете написать мне @da1grn. В том числе, если вы хотели бы видеть какой нибудь класс тестов, но не хотите писать его вручную, то можете не стесняться написать мне.

В данный момент, реализация автора проходит все тесты. В случае, если у вас тесты не проходят, попробуйте проверить свой код на надежность перед тем как писать мне.

Далее есть некоторые пояснения о том, как работать с тестами:

## ❓ Как запускать тесты?

Скопируйте папку integration_tests в корень проекта, в котором лежат исходники кода из папки hw2 репозитория Романа Вячеславовича. Проверьте, что имеются все файлы из этой папки.
Запустите тесты из командной строки:
```bash
python3 integration_tests/tests_runner.py
```

## 📂 Структура

### 1. `assembler.py`
Это упрощенный **Ассемблер**, написанный на Python.
*   **Что делает:** Читает текстовый файл с инструкциями (RISC-V Assembly) и превращает его в машинный код (строки из нулей и единиц), понятный вашему модулю `memory.v`.
*   **Особенности:**
    *   Поддерживает метки (labels) для переходов (например, `loop:`, `end:`).
    *   Автоматически рассчитывает смещения для ветвлений (`beq`, `jal` и т.д.).
    *   Понимает синтаксис вида `sw x1, 4(x2)` и `sw x1, x2, 4` (упрощенный).

### 2. `tests_runner.py`
Это **Скрипт запуска и проверки**.
*   **Что делает:**
    1.  Берет сценарий теста (ассемблерный код + ожидаемые значения регистров).
    2.  Генерирует временный файл `temp.asm`.
    3.  Вызывает `assembler.py`, чтобы создать `instructions.dat`.
    4.  Компилирует ваш Verilog проект с помощью `iverilog`.
    5.  Запускает симуляцию (`cpu_test`).
    6.  Парсит вывод симулятора и сверяет итоговые значения регистров и памяти с ожидаемыми.
*   **Зачем нужен:** Позволяет за секунду проверить работоспособность процессора на десятке сложных программ, не запуская GTKWave вручную.

---

## 🧪 Описание тестов

Набор тестов (`test_cases`) покрывает различные аспекты архитектуры:

1.  **Arithmetic**: Базовая арифметика (`add`, `sub`, `and`, `or`, `slt`, `addi`, `lui`). Проверяет работу ALU.
2.  **Memory**: Запись (`sw`) и чтение (`lw`). Проверяет корректность вычисления адресов.
3.  **Branch**: Условные переходы (`beq`, `bne`, `blt`). Проверяет логику пропуска инструкций.
4.  **Jump**: Безусловные прыжки (`jal`, `jalr`).
5.  **LoopSum**: **Цикл**. Суммирует числа от 1 до 10. Проверяет переходы "назад" (отрицательное смещение) и работу процессора на протяжении множества тактов.
6.  **SignedOps**: Знаковые операции. Важен для проверки того, правильно ли процессор понимает разницу между `-1` (0xFFFFFFFF) и `1`, особенно в операциях сравнения (`slt`, `blt`).
7.  **LargeConst**: Загрузка больших чисел. Проверяет комбинацию `lui` (старшие биты) + `addi` (младшие биты).
8.  **FullInstructionSet**: "Стресс-тест". Использует **все** доступные инструкции в одной программе.
9.  **ComplexLogic**: Работа с массивами в памяти (заполнение циклами и последующее чтение).

---

## 🛠 Как добавить свой тест

Вы можете легко добавить собственные сценарии проверки. Для этого откройте файл `tests_runner.py` и найдите список `test_cases`.

Добавьте новый элемент в этот список по следующему шаблону:

```python
    {
        "name": "MyNewTest",  # Название теста
        "asm": """
            # Ваш код на ассемблере
            addi x1, x0, 5
            addi x2, x0, 3
            add x3, x1, x2
        """,
        "checks": {
            # Ожидаемые значения после выполнения
            "x3": 8,          # Проверка регистра x3(Доступны x0...x31)
            # "x1": 5,         # Можно проверить несколько регистров
            "mem[0]": 0,      # Можно проверять ячейки памяти (по байтовому адресу)
        }
    },
```

---

## ⚠️ Особенности и нюансы реализации

В ходе разработки тестов мы выявили несколько важных моментов, касающихся текущей реализации процессора (на основе вашего ТЗ):

1.  **Запись в x0**: В классическом RISC-V регистр `x0` всегда равен 0 и запись в него игнорируется. В текущей реализации (файл `register_file.v`) **запись в x0 возможна**.
    *   *Как это влияет на тесты:* В тестах мы стараемся не использовать `x0` как "мусорную корзину" (destination register), если планируем потом полагаться на то, что там ноль. Например, в `jalr` мы пишем адрес возврата в `x17`, а не в `x0`.
2.  **Время симуляции**: В файле `cpu_test.v` задано количество тактов симуляции (сейчас 100).
    *   *Совет:* Если вы напишете очень длинный цикл, тест может упасть не потому что процессор сломан, а потому что симуляция закончилась раньше времени. Если нужно, увеличьте цикл `for` в `cpu_test.v`.
3.  **Знаковые числа**: Python и Verilog по-разному могут выводить 32-битные числа (как -1 или как 4294967295). Тест-раннер старается это учитывать, но иногда проще ожидать unsigned значение в блоке `checks`.

---

## 💡 Советы (special from gemini)

*   **Если тест упал:** Скрипт покажет вам `Debug Output` с состоянием регистров по тактам. Посмотрите, на каком моменте значение регистра отклонилось от ожидаемого.
*   **GTKWave — ваш друг:** Если по логам непонятно, *почему* значение неверное, откройте файл `dump_unit_tests.vcd` (или добавьте дамп в `cpu_test.v`) в GTKWave. Там видно все сигналы внутри процессора.


Удачи в покорении RISC-V! 🚀