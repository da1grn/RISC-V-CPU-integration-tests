# Интеграционные тесты для RISC-V процессора (Fuzzing Edition)

Эта директория содержит систему автоматизированного динамического тестирования (фаззинг) вашего процессора. В отличие от обычных тестов, программы здесь не фиксированы — они генерируются случайно при каждом запуске, что позволяет проверить тысячи уникальных комбинаций инструкций.

## 📂 Состав инструментов

### 1. `assembler.py`
Мини-ассемблер для подмножества RISC-V.
*   Превращает текстовый `.asm` код в бинарный формат для модуля `memory.v`.
*   Обрабатывает метки и автоматически вычисляет смещения для `beq`, `bne`, `blt`, `jal`.

### 2. `tests_runner.py`
Ядро тестовой системы. Он содержит в себе:
*   **ReferenceCPU**: Высокоточный эмулятор процессора на Python. Он не просто считает математику, а эмулирует 32-битную арифметику (с переполнениями), знаковые/беззнаковые преобразования и работу памяти бит-в-бит как в Verilog.
*   **Генераторы (Fuzzers)**: Создают случайные последовательности команд.
*   **Автоматизацию**: Сам запускает `iverilog`, проводит симуляцию и сверяет дампы регистров.

---

## 🧪 Стратегии тестирования

Скрипт запускает 100 тестов, разделенных на 4 группы:

1.  **ALU Chaos (30 тестов)**: 
    *   **Что делает:** Генерирует цепочки случайных вычислений (`add`, `sub`, `and`, `or`, `slt`) между случайными регистрами (x1-x15).
    *   **Особенности:** Использует сложные паттерны инициализации (0, -1, MAX_INT, 0xAAAAAAAA) для проверки работы битовых операций и знакового расширения. Проверяет корректность работы ALU при переполнениях.

2.  **Memory Walk (30 тестов)**: 
    *   **Что делает:** Симулирует работу с указателями. Базовый регистр-указатель случайно перемещается по памяти, записывая и считывая данные по разным смещениям.
    *   **Особенности:** Проверяет корректность вычисления адресов в инструкциях `lw` и `sw`, а также целостность данных при перезаписи ячеек.

3.  **Branch Maze (30 тестов)**: 
    *   **Что делает:** Создает "лабиринт" из ветвлений. Процессор должен принимать верные решения (`beq`, `bne`, `blt`) на основе сравнения случайных чисел, чтобы дойти до конца.
    *   **Особенности:** Проверяет логику условных переходов, в том числе сравнение знаковых чисел (`blt`).

4.  **Jump PingPong (10 тестов)**: 
    *   **Что делает:** Запутанная последовательность безусловных прыжков (`jal`) по разным меткам.
    *   **Особенности:** Проверяет корректность обновления `PC` и сохранения адреса возврата.

---

## 🚀 Запуск тестов

Скопируйте содержимое в корень проекта, в котором лежат исходники кода из папки hw2 репозитория курса. Проверьте, что имеются все файлы из этой папки.
Запустите тесты из командной строки:
```bash
python3 integration_tests/tests_runner.py
```

При каждом запуске генерируется **новый** набор программ.

---

## 🔍 Технические детали реализации

*   **ReferenceCPU**: Реализован класс на Python, который точно моделирует поведение `register_file.v` и 32-битного ALU (с усечением результатов).
*   **Время симуляции**: Время выполнения теста в `cpu_test.v` увеличено до 500 тактов, чтобы гарантировать завершение сложных циклов и переходов.
*   **Диапазоны**: Генераторы используют безопасные диапазоны адресов памяти (0-8КБ) и корректные 12-битные константы, чтобы избежать артефактов ассемблирования.

---

## 🛠 Модификация

Вы можете подстроить сложность тестов под свои нужды в файле `tests_runner.py` (блок `Main Runner` в конце файла):
*   Изменить количество тестов (аргумент функций `gen_...`).
*   Изменить набор инструкций в `ALU Chaos` (список `ops`).
